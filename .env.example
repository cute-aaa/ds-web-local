# === 可选：本地 LLM 后端（双模式，默认不启用）===
DEEPSEEK_API_KEY=
LLM_BASE_URL=https://api.deepseek.com
LLM_MODEL=deepseek-chat

# === 认证（默认关闭，本地单机用）===
AUTH_TOKEN=

# === 桥接脚本默认后端地址 ===
BRIDGE_BACKEND_URL=http://localhost:8088
